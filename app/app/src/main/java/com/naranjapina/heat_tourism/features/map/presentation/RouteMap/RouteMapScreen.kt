package com.naranjapina.heat_tourism.features.map.presentation.RouteMap

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.navigation.NavHostController

/**
 * TODO Persona B (Bloque B) — Mapa de la ruta activa en vivo.
 *
 * Distinto a MapScreen (mapa general): aqui se muestra la ruta del viaje activo,
 * la ubicacion del usuario y, si user.tipo == COORDINATOR, la ubicacion de
 * todos los miembros del grupo en tiempo real.
 */
@Composable
fun RouteMapScreen(navController: NavHostController) {
    Text("TODO: RouteMapScreen — implementacion en Bloque B (tiempo real)")
}
