[project]
name = "heattourism"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cocoindex-code-mcp-server>=0.2.0",
    "sentence-transformers>=5.5.1",
]
